import 'package:flutter/material.dart';

class FocusGame extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Focus Game')),
      body: Center(
        child: Text('Welcome to Focus Game!'),
      ),
    );
  }
}